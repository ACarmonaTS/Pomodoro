import { StyleSheet, } from "react-native";

const styles = StyleSheet.create({
  itemStyle: {
    width: "33.3%",

    padding: 10,
  },

  itemText: {
    fontWeight: "500",
    fontSize: 18,
  }
});

export default styles;
